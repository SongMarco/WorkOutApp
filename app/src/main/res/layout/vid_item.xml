<?xml version="1.0" encoding="utf-8"?>
<nova.workoutapp22.listviewSrcForVid.VidItemLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/imageViewVid"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1.5"
            app:srcCompat="@drawable/basicimage" />

        <TextView
            android:id="@+id/textViewVid"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center"
            android:text="VideoTitle"
            android:textAlignment="center" />

        <CheckBox
            android:id="@+id/checkBoxVid"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="gone"

            android:buttonTint="@color/colorPrimary"
            android:clickable="false"
            android:focusable="false"
            app:theme="@style/Theme.AppCompat.Light.NoActionBar"
            />
    </LinearLayout>
</nova.workoutapp22.listviewSrcForVid.VidItemLayout>